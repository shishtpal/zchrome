# Chrome Extensions

```bash
zchrome extensions load /path/to/extension   # Load unpacked extension
zchrome extensions list                       # List loaded extensions
zchrome extensions unload /path/to/extension  # Remove extension
zchrome open                                  # Restart Chrome with extensions
```

Extensions require headed mode (headless is auto-disabled). Not compatible with cloud providers.
