# Interactive REPL

```bash
zchrome interactive
```

Enters an interactive shell where all commands work without the `zchrome` prefix:

```
zchrome> navigate https://example.com
zchrome> snapshot -i
zchrome> click @e3
zchrome> fill @e5 "hello"
zchrome> screenshot --output test.png
```
